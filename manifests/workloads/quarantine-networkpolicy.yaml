apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: quarantine-deny-all
spec:
  podSelector:
    matchLabels:
      security.watergon/quarantine: "true"
  policyTypes:
    - Ingress
    - Egress
