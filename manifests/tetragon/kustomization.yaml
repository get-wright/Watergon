apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Tetragon itself installed via Helm (values.yaml).
# This kustomization adds optional TracingPolicy CRDs for kernel-level hooks
# beyond default exec/exit (e.g. tcp_connect, file open).
resources:
  - tracingpolicies.yaml
